generated: '2026-07-19'
method: generated
source: >-
  Generated by the API Evangelist enrichment pipeline from
  openapi/konfetti-store-openapi.yml. Every operationId referenced in these
  skills exists in that specification, which was itself built from live probes
  of https://api.gokonfetti.com/v1. konfetti publishes no Agent Skills, no
  AGENTS.md and no llms.txt — searches of its hosts found none.
description: >-
  Packaged Agent Skills for the konfetti Store API. Both skills are read-only
  and cover the public, unauthenticated catalog surface. No skill is provided
  for checkout, orders, profile or the lead-capture endpoints: those require a
  Bearer token that konfetti publishes no way to obtain, or they write real
  records non-idempotently.

searched:
  - target: https://gokonfetti.com/AGENTS.md
    result: not found
  - target: https://gokonfetti.com/llms.txt
    result: 404
  - target: konfetti public surface
    result: no provider-published agent skills or agent documentation

skills:
  - name: konfetti-search-experiences
    file: konfetti-search-experiences.md
    description: >-
      Search and browse konfetti's catalog of 7,600+ bookable experiences,
      workshops and courses across Germany and Austria — filtering by category,
      supplier, type and availability, and reading full experience detail
      including pricing, ratings and the next available date.
    operations: [listStoreCategories, listCategories, listEvents, getEvent, listSimilarEvents]
    auth_required: false
    writes: false
  - name: konfetti-supplier-catalog
    file: konfetti-supplier-catalog.md
    description: >-
      Build a picture of a konfetti partner host (supplier) — their full
      experience catalog, pricing range, review standing and geographic
      footprint — by pivoting from any single experience to everything that
      supplier offers.
    operations: [listEvents, getEvent, listSimilarEvents]
    auth_required: false
    writes: false

excluded:
  - reason: >-
      Require a Bearer token with no publicly documented way to obtain one.
    operations: [calculateEventPricing, validateCart, validateCoupon, listOrders, getUserProfile, issueAccessToken, registerCustomer]
  - reason: >-
      Non-idempotent writes that create real sales leads and subscriptions in
      konfetti's systems; unsafe for agent retry.
    operations: [createDateRequest, createPrivateEventRequest, subscribeNewsletter, subscribeToEvent]
  - reason: Returned 500/404/422 when probed; too unstable to ground a skill in.
    operations: [listEventAddOns, getEventCalendar, getSupplierSubdomain, recordMetric]

caveat: >-
  These skills target an undocumented internal API. konfetti publishes no terms
  of use covering programmatic access, no rate limits, no versioning policy and
  no support channel for developers. See lifecycle/konfetti-lifecycle.yml.

related:
  openapi: openapi/konfetti-store-openapi.yml
  conventions: conventions/konfetti-conventions.yml
  errors: errors/konfetti-problem-types.yml
  mcp: mcp/konfetti-mcp.yml
